first name:
  required: true
  parser:
  present: true

last name:
  required: true
  parser:
  present: true

email:
  required: true
  parser: 'is_email'
  present: true

phone:
  required: false
  parser: 'is_phone'
  present: false

zip codes:
  required: false
  parser:
  present: false

price point:
  required: false
  parser:
  present: false

language preference:
  required: false
  parser: 'is_locale'
  present: false

max loan amount:
  required: false
  parser:
  present: false

downpayment:
  required: false
  parser:
  present: false

max home price:
  required: false
  parser:
  present: false

estimated rate:
  required: false
  parser: 'is_interest_rate'
  present: false

loan type:
  required: false
  parser:
  present: false

loan term months:
  required: false
  parser:
  present: false

loan adjustable:
  required: false
  parser:
  present: false

loan arm years initial:
  required: false
  parser:
  present: false

expires at:
  required: false
  parser: 'is_date'
  present: false

preapproved:
  required: false
  parser: 'is_boolean'
  present: false
